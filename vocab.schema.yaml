# Rime schema
# encoding: utf-8

schema:
  schema_id: vocab
  name: Vocabulary
  version: "0.1"


engine:
  processors:
  segmentors:
  translators:
    - script_translator
  filters:

translator:
  dictionary: vocab